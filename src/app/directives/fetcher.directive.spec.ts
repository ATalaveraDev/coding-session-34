import { FetcherDirective } from './fetcher.directive';

describe('FetcherDirective', () => {
  it('should create an instance', () => {
    const directive = new FetcherDirective();
    expect(directive).toBeTruthy();
  });
});
